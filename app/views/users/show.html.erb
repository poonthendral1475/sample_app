<% provide(:title, @user.name) %>
<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <h1>
        <%= gravatar_for @user %>
        <span class="name"><%= @user.name %></span>
      </h1>
      <div class="d-flex followup">
        <p>folowers</p>
        <p>following</p>
      </div>
    </section>
  </aside>
</div>